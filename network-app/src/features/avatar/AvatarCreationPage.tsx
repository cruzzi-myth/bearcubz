import { RouteShell } from '../../components/RouteShell';

export function AvatarCreationPage() {
  return (
    <RouteShell
      eyebrow="Identity"
      title="Avatar Creation"
      description="Build the version of yourself that walks the galaxy. Character customization is a Phase 3+ system — this route exists so the flow from Passport → Avatar → Dashboard is already wired."
    />
  );
}
